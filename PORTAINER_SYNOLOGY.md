# 🐳 คู่มือติดตั้งผ่าน Portainer บน Synology NAS

คู่มือนี้จะช่วยคุณติดตั้งระบบลงทะเบียนเข้าร่วมประชุมผ่าน Portainer บน Synology NAS แบบละเอียดทุกขั้นตอน

---

## 📋 สิ่งที่ต้องเตรียม

- ✅ Synology NAS ที่มี Docker Package ติดตั้งแล้ว
- ✅ Portainer ติดตั้งและรันอยู่แล้ว (ปกติจะอยู่ที่ port 9000)
- ✅ ไฟล์โปรเจ็กต์ทั้งหมด

---

## 🚀 วิธีติดตั้ง (แบบละเอียด)

### ขั้นตอนที่ 1: อัปโหลดไฟล์ไปยัง Synology NAS

1. **เปิด File Station** ใน Synology DSM

2. **สร้างโฟลเดอร์ใหม่**:
   - ไปที่ `/docker/` (หรือ `/volume1/docker/`)
   - สร้างโฟลเดอร์ชื่อ `meeting-registration`

3. **อัปโหลดไฟล์ทั้งหมด** ไปยัง `/docker/meeting-registration/`:
   ```
   ✅ Dockerfile
   ✅ docker-compose.portainer.yml (หรือ docker-compose.yml)
   ✅ package.json
   ✅ package-lock.json
   ✅ server.js
   ✅ database.js
   ✅ index.html
   ✅ admin.html
   ✅ admin-login.html
   ✅ styles.css
   ✅ admin.css
   ✅ script.js
   ✅ admin.js
   ✅ .dockerignore
   ✅ env.example (สำหรับอ้างอิง)
   ```

   **หมายเหตุ**: ไม่ต้องอัปโหลด:
   - `node_modules/` (จะถูกสร้างใน Docker)
   - `data/` (จะถูกสร้างใน volume)
   - `.env` (จะตั้งค่าผ่าน Portainer)

---

### ขั้นตอนที่ 2: เปิด Portainer

1. เปิดเบราว์เซอร์และไปที่:
   ```
   http://your-nas-ip:9000
   ```
   หรือ
   ```
   https://your-nas-ip:9443
   ```

2. **Login** เข้าสู่ระบบ Portainer

---

### ขั้นตอนที่ 3: สร้าง Stack

#### วิธีที่ 1: ใช้ Web Editor (แนะนำ - ง่ายที่สุด)

1. ไปที่ **Stacks** ในเมนูด้านซ้าย
2. คลิก **Add stack** (ปุ่มสีเขียว)
3. ตั้งค่า:
   - **Name**: `meeting-registration`
   - **Build method**: เลือก **Web editor**
4. **คัดลอกเนื้อหา** จากไฟล์ `docker-compose.portainer.yml` แล้ววางใน editor
5. **แก้ไข Environment Variables** (ถ้าต้องการ):
   ```yaml
   environment:
     - PORT=3030
     - ADMIN_PASSWORD=your_secure_password_here
     - SESSION_SECRET=your_random_secret_key_here
   ```
6. คลิก **Deploy the stack**

#### วิธีที่ 2: อัปโหลดไฟล์

1. ไปที่ **Stacks** → **Add stack**
2. ตั้งค่า:
   - **Name**: `meeting-registration`
   - **Build method**: เลือก **Upload**
3. คลิก **Select a file to upload**
4. เลือกไฟล์ `docker-compose.portainer.yml` ที่อัปโหลดไว้ใน NAS
5. **แก้ไข Environment Variables** (ถ้าต้องการ)
6. คลิก **Deploy the stack**

#### วิธีที่ 3: ใช้ Git Repository (ถ้ามี)

1. ไปที่ **Stacks** → **Add stack**
2. ตั้งค่า:
   - **Name**: `meeting-registration`
   - **Build method**: เลือก **Repository**
3. กรอกข้อมูล Git repository
4. ระบุ path ของ `docker-compose.portainer.yml`
5. คลิก **Deploy the stack**

---

### ขั้นตอนที่ 4: ตรวจสอบการ Deploy

1. ไปที่ **Stacks** → `meeting-registration`
2. ตรวจสอบว่า container มีสถานะ **Running** (สีเขียว)
3. คลิกที่ container name เพื่อดูรายละเอียด
4. ไปที่แท็บ **Logs** เพื่อตรวจสอบว่าไม่มี error

**ตัวอย่าง Log ที่ถูกต้อง**:
```
✅ Connected to SQLite database
✅ Database initialized
✅ Server is running on http://localhost:3030
```

---

### ขั้นตอนที่ 5: เข้าถึงระบบ

เปิดเบราว์เซอร์และไปที่:

- **หน้าแรก (ลงทะเบียน)**: 
  ```
  http://your-nas-ip:3030
  ```

- **หน้า Admin Login**: 
  ```
  http://your-nas-ip:3030/admin-login.html
  ```

- **รหัสผ่าน Admin** (default): `admin123`
  - **⚠️ ควรเปลี่ยนทันที!** (ดูด้านล่าง)

---

## ⚙️ การตั้งค่า Environment Variables

### ผ่าน Portainer UI (แนะนำ)

1. ไปที่ **Stacks** → `meeting-registration`
2. คลิก **Editor** (ปุ่มแก้ไข)
3. แก้ไขส่วน `environment`:
   ```yaml
   environment:
     - PORT=3030
     - ADMIN_PASSWORD=your_very_secure_password_here
     - SESSION_SECRET=generate_random_string_here_min_32_chars
   ```
4. คลิก **Update the stack**

### สร้าง Session Secret ที่ปลอดภัย

ใช้คำสั่งนี้ใน terminal:
```bash
openssl rand -base64 32
```

หรือใช้ online generator: https://randomkeygen.com/

---

## 📁 การจัดการ Volumes (ข้อมูล)

### ตรวจสอบ Volume

1. ไปที่ **Volumes** ใน Portainer
2. ควรเห็น volume ชื่อ `meeting-registration-data`
3. ข้อมูล SQLite database จะถูกเก็บใน volume นี้

### Backup ข้อมูล

1. ไปที่ **Volumes** → `meeting-registration-data`
2. คลิก **Browse** เพื่อดูไฟล์
3. Download ไฟล์ `meeting-registration.db` เพื่อ backup

### Restore ข้อมูล

1. อัปโหลดไฟล์ `.db` กลับไปยัง volume
2. Restart container

---

## 🔄 การอัปเดต

### วิธีที่ 1: ผ่าน Portainer UI (แนะนำ)

1. **อัปโหลดไฟล์ใหม่** ไปยัง `/docker/meeting-registration/`
2. ไปที่ **Stacks** → `meeting-registration`
3. คลิก **Editor**
4. คลิก **Update the stack**
   - Portainer จะ rebuild image อัตโนมัติ

### วิธีที่ 2: Rebuild Image แยก

1. ไปที่ **Images** ใน Portainer
2. คลิก **Build a new image**
3. ตั้งค่า:
   - **Name**: `meeting-registration:latest`
   - **Build method**: **Upload**
   - อัปโหลด `Dockerfile` และไฟล์ที่จำเป็น
4. หลังจาก build สำเร็จ → ไปที่ **Stacks** → **Editor** → **Update**

---

## 🌐 การตั้งค่า Reverse Proxy (แนะนำ)

เพื่อให้เข้าถึงผ่าน domain name แทน IP:port

### ผ่าน Synology Reverse Proxy

1. เปิด **Control Panel** → **Login Portal** → **Advanced** → **Reverse Proxy**
2. คลิก **Create**
3. ตั้งค่า:
   - **Description**: `Meeting Registration`
   - **Source**:
     - Protocol: `HTTPS`
     - Hostname: `meeting.yourdomain.com`
     - Port: `443`
   - **Destination**:
     - Protocol: `HTTP`
     - Hostname: `localhost`
     - Port: `3030`
4. ตั้งค่า SSL Certificate
5. คลิก **Save**

### ผ่าน Portainer (ถ้าใช้ Traefik)

เพิ่ม labels ใน `docker-compose.portainer.yml`:
```yaml
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.meeting.rule=Host(`meeting.yourdomain.com`)"
  - "traefik.http.routers.meeting.entrypoints=websecure"
  - "traefik.http.routers.meeting.tls.certresolver=letsencrypt"
```

---

## 📊 Monitoring และ Logs

### ดู Logs

1. ไปที่ **Stacks** → `meeting-registration`
2. คลิกที่ container name
3. ไปที่แท็บ **Logs**
4. สามารถ filter และ search logs ได้

### ดู Statistics

1. ไปที่ **Stacks** → `meeting-registration`
2. คลิกที่ container name
3. ไปที่แท็บ **Stats**
4. ดู CPU, Memory, Network usage

### ดู Health Status

1. ไปที่ **Stacks** → `meeting-registration`
2. ดู Health status indicator (สีเขียว = healthy)

---

## 🔒 Security Best Practices

### 1. เปลี่ยนรหัสผ่าน Admin

**สำคัญมาก!** เปลี่ยนทันทีหลังจาก deploy:

1. ไปที่ **Stacks** → **Editor**
2. แก้ไข `ADMIN_PASSWORD`
3. **Update the stack**

### 2. เปลี่ยน Session Secret

ใช้ random string ที่ยาวอย่างน้อย 32 characters

### 3. ใช้ HTTPS

ตั้งค่า Reverse Proxy พร้อม SSL Certificate

### 4. Firewall Rules

- เปิด port 3030 เฉพาะ IP ที่ต้องการ (ถ้าไม่ใช้ Reverse Proxy)
- หรือใช้ Reverse Proxy และเปิดเฉพาะ port 80/443

### 5. จำกัดการเข้าถึง Portainer

- ตั้งค่า firewall สำหรับ port 9000/9443
- ใช้ strong password
- เปิดใช้งาน 2FA (ถ้ามี)

---

## 🛠️ การแก้ไขปัญหา

### Container ไม่เริ่มต้น

1. **ตรวจสอบ Logs**:
   - ไปที่ **Stacks** → container → **Logs**
   - ดู error messages

2. **ตรวจสอบ Port**:
   - ตรวจสอบว่า port 3030 ไม่ถูกใช้งาน
   - เปลี่ยน port ใน environment variables ถ้าจำเป็น

3. **ตรวจสอบ Volumes**:
   - ไปที่ **Volumes**
   - ตรวจสอบว่า volume สร้างสำเร็จ

### ไม่สามารถเข้าถึงได้

1. **ตรวจสอบ Container Status**:
   - ควรเป็น **Running** (สีเขียว)

2. **ตรวจสอบ Port Mapping**:
   - ไปที่ **Stacks** → **Editor**
   - ตรวจสอบ port mapping: `"3030:3030"`

3. **ตรวจสอบ Firewall**:
   - Control Panel → Security → Firewall
   - เปิด port 3030

4. **ทดสอบจาก NAS**:
   ```bash
   curl http://localhost:3030
   ```

### ข้อมูลหาย

1. **ตรวจสอบ Volume**:
   - ไปที่ **Volumes** → `meeting-registration-data`
   - ตรวจสอบว่า volume มีข้อมูล

2. **ตรวจสอบ Permissions**:
   - ตรวจสอบ logs สำหรับ permission errors

3. **Backup ข้อมูล**:
   - Download volume เพื่อ backup

### Health Check ล้มเหลว

1. **ตรวจสอบ Logs**:
   - ดู error messages

2. **ตรวจสอบ Network**:
   - ตรวจสอบว่า container สามารถเข้าถึง localhost:3030 ได้

3. **ปิด Health Check ชั่วคราว**:
   - แก้ไข `docker-compose.portainer.yml`
   - Comment out healthcheck section

---

## 📝 Checklist

- [ ] อัปโหลดไฟล์ทั้งหมดไปยัง NAS
- [ ] สร้าง Stack ใน Portainer
- [ ] ตั้งค่า environment variables (ADMIN_PASSWORD, SESSION_SECRET)
- [ ] Deploy stack สำเร็จ
- [ ] ตรวจสอบ logs ไม่มี error
- [ ] ทดสอบเข้าถึงผ่าน IP:port
- [ ] เปลี่ยนรหัสผ่าน Admin
- [ ] ตั้งค่า Reverse Proxy (ถ้าต้องการ)
- [ ] ตั้งค่า SSL Certificate (ถ้าใช้ HTTPS)
- [ ] ทดสอบระบบทั้งหมด
- [ ] Backup ข้อมูล

---

## 🎯 ขั้นตอนต่อไป

1. ✅ Deploy สำเร็จ → ทดสอบระบบ
2. 🔒 ตั้งค่า Security → เปลี่ยนรหัสผ่าน, SSL
3. 📊 ตั้งค่า Monitoring → Logs, Alerts
4. 🔄 ตั้งค่า Backup → Backup data volume
5. 📱 แชร์ลิงก์ให้ผู้ใช้

---

## 💡 Tips

- ใช้ **Named Volumes** สำหรับ data persistence
- ตั้งค่า **Auto-restart** policy (มีอยู่แล้ว: `restart: unless-stopped`)
- ใช้ **Health checks** เพื่อตรวจสอบสถานะ container
- ตั้งค่า **Resource limits** ถ้าต้องการจำกัดการใช้ทรัพยากร
- ใช้ **Portainer Templates** เพื่อ deploy ใหม่ได้ง่าย
- **Backup ข้อมูล** เป็นประจำ

---

## 📚 เอกสารเพิ่มเติม

- [Portainer Documentation](https://docs.portainer.io/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Synology Docker Guide](https://kb.synology.com/en-global/DSM/help/Docker/docker_desc)

---

## ❓ คำถามที่พบบ่อย

### Q: ต้องใช้ port อะไร?
**A**: Default คือ 3030 แต่สามารถเปลี่ยนได้ใน environment variables

### Q: ข้อมูลจะหายไหมถ้า restart container?
**A**: ไม่หาย เพราะใช้ named volume ที่ persistent

### Q: ต้อง rebuild image ทุกครั้งที่แก้ไข code ไหม?
**A**: ใช่ ต้อง rebuild image หรือใช้ `docker-compose up --build`

### Q: ใช้กับ domain name ได้ไหม?
**A**: ได้ ต้องตั้งค่า Reverse Proxy ใน Synology

### Q: Backup ข้อมูลอย่างไร?
**A**: Download volume `meeting-registration-data` ผ่าน Portainer

---

**🎉 ขอให้ใช้งานได้อย่างราบรื่น!**

