# 🐳 คู่มือติดตั้งผ่าน Portainer บน Synology NAS

## 📋 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: เตรียมไฟล์

1. **สร้างโฟลเดอร์ใน Synology NAS**
   - เปิด **File Station**
   - สร้างโฟลเดอร์ใหม่: `/docker/meeting-registration`
   - หรือใช้ path อื่นที่ต้องการ

2. **อัปโหลดไฟล์ทั้งหมด**
   - อัปโหลดไฟล์ต่อไปนี้ไปยัง `/docker/meeting-registration/`:
     ```
     Dockerfile
     docker-compose.yml
     package.json
     server.js
     database.js
     index.html
     admin.html
     admin-login.html
     styles.css
     admin.css
     script.js
     admin.js
     env.example (optional)
     ```

### ขั้นตอนที่ 2: เปิด Portainer

1. เปิดเบราว์เซอร์
2. ไปที่ `http://your-nas-ip:9000`
   - หรือ `https://your-nas-ip:9443` (ถ้าใช้ HTTPS)
3. Login เข้าสู่ระบบ Portainer

### ขั้นตอนที่ 3: สร้าง Stack

1. **ไปที่ Stacks**
   - คลิก **Stacks** ในเมนูด้านซ้าย
   - หรือไปที่ **Home** → **Stacks**

2. **สร้าง Stack ใหม่**
   - คลิกปุ่ม **Add stack** (สีเขียว)
   - ตั้งชื่อ: `meeting-registration`

3. **เลือกวิธีการ Deploy**
   
   **วิธีที่ 1: Web Editor (แนะนำ)**
   - เลือก **Web editor**
   - คัดลอกเนื้อหาจากไฟล์ `docker-compose.yml` วางใน editor
   - หรือใช้เนื้อหาด้านล่าง:

   **วิธีที่ 2: Upload**
   - เลือก **Upload**
   - อัปโหลดไฟล์ `docker-compose.yml`

4. **ตั้งค่า Environment Variables** (ใน Web editor)
   
   แก้ไขส่วน `environment`:
   ```yaml
   environment:
     - PORT=3030
     - NODE_ENV=production
     - ADMIN_PASSWORD=your_secure_password_here
     - SESSION_SECRET=your_random_secret_key_here
   ```
   
   **สำคัญ**: เปลี่ยน `ADMIN_PASSWORD` และ `SESSION_SECRET` ให้เป็นค่าที่ปลอดภัย

5. **Deploy**
   - คลิกปุ่ม **Deploy the stack** (สีเขียว)
   - รอให้ build และ deploy เสร็จ (อาจใช้เวลาสักครู่)

### ขั้นตอนที่ 4: ตรวจสอบการ Deploy

1. **ดู Logs**
   - ไปที่ **Stacks** → `meeting-registration`
   - คลิกที่ container name: `meeting-registration-app`
   - ไปที่แท็บ **Logs**
   - ตรวจสอบว่าไม่มี error และเห็นข้อความ:
     ```
     ✅ Server is running on http://localhost:3030
     ✅ Connected to SQLite database
     ```

2. **ตรวจสอบสถานะ**
   - ไปที่ **Containers**
   - ตรวจสอบว่า container `meeting-registration-app` มีสถานะ **Running** (สีเขียว)

### ขั้นตอนที่ 5: ทดสอบการเข้าถึง

1. **เปิดเบราว์เซอร์**
2. ไปที่:
   - **Frontend**: `http://your-nas-ip:3030`
   - **Admin Login**: `http://your-nas-ip:3030/admin-login.html`
3. **Login Admin**:
   - รหัสผ่าน: `admin123` (หรือที่ตั้งค่าไว้)

---

## 🔧 การตั้งค่าเพิ่มเติม

### เปลี่ยนรหัสผ่าน Admin

1. ไปที่ **Stacks** → `meeting-registration` → **Editor**
2. แก้ไข `ADMIN_PASSWORD` ในส่วน environment
3. คลิก **Update the stack**

### เปลี่ยน Port

1. แก้ไขใน `docker-compose.yml`:
   ```yaml
   ports:
     - "3031:3030"  # เปลี่ยน 3031 เป็น port ที่ต้องการ
   ```
2. Update stack

### ตั้งค่า Reverse Proxy (แนะนำ)

1. เปิด **Control Panel** → **Login Portal** → **Reverse Proxy**
2. คลิก **Create**
3. ตั้งค่า:
   - **Source**: `meeting.yourdomain.com:443` (HTTPS)
   - **Destination**: `localhost:3030` (HTTP)
4. ใช้ SSL Certificate

---

## 📊 การจัดการ Stack

### ดู Logs

1. **Stacks** → `meeting-registration` → คลิก container name
2. ไปที่แท็บ **Logs**

### Restart Container

1. **Stacks** → `meeting-registration`
2. คลิก **Restart** (ไอคอนรีเฟรช)

### Stop/Start Stack

1. **Stacks** → `meeting-registration`
2. คลิก **Stop** หรือ **Start**

### Update Stack

1. **Stacks** → `meeting-registration` → **Editor**
2. แก้ไข `docker-compose.yml`
3. คลิก **Update the stack**

### Delete Stack

1. **Stacks** → `meeting-registration`
2. คลิก **Remove** (ระวัง: จะลบ container และ volumes)

---

## 💾 Data Persistence

ข้อมูลจะถูกเก็บใน Docker Volume ชื่อ `meeting-registration_meeting-data`

### Backup Data

1. ไปที่ **Volumes** ใน Portainer
2. คลิก volume `meeting-registration_meeting-data`
3. คลิก **Download** เพื่อ backup

### Restore Data

1. อัปโหลดไฟล์ backup ไปยัง volume

---

## 🛠️ การแก้ไขปัญหา

### Container ไม่เริ่มต้น

1. ดู **Logs** ใน Portainer
2. ตรวจสอบ error messages
3. ตรวจสอบว่า port ไม่ถูกใช้งาน

### ไม่สามารถเข้าถึงได้

1. ตรวจสอบว่า container **Running**
2. ตรวจสอบ port mapping
3. ตรวจสอบ firewall rules

### Build Error

1. ตรวจสอบว่า `Dockerfile` ถูกต้อง
2. ตรวจสอบว่าไฟล์ทั้งหมดอัปโหลดครบ
3. ดู build logs ใน Portainer

---

## ✅ Checklist

- [ ] อัปโหลดไฟล์ทั้งหมดไปยัง NAS
- [ ] เปิด Portainer
- [ ] สร้าง Stack ใหม่
- [ ] ตั้งค่า environment variables
- [ ] Deploy stack
- [ ] ตรวจสอบ logs
- [ ] ทดสอบเข้าถึง
- [ ] เปลี่ยนรหัสผ่าน Admin
- [ ] ตั้งค่า Reverse Proxy (optional)

---

## 🎯 หลังจาก Deploy สำเร็จ

1. ✅ ทดสอบระบบทั้งหมด
2. 🔒 เปลี่ยนรหัสผ่าน Admin
3. 📊 ตั้งค่า Monitoring
4. 🔄 ตั้งค่า Backup

---

**ดูรายละเอียดเพิ่มเติม**: [PORTAINER_DEPLOY.md](./PORTAINER_DEPLOY.md)

