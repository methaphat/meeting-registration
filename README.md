# ระบบลงทะเบียนเข้าร่วมประชุม

เว็บแอปพลิเคชันสำหรับลงทะเบียนเข้าร่วมประชุม พร้อมแสดงสถิติและรายชื่อผู้ลงทะเบียน โดยมี Backend สำหรับจัดการข้อมูลและเก็บข้อมูลใน SQLite Database

## คุณสมบัติ

- ✅ ฟอร์มลงทะเบียน (ชื่อ-นามสกุล, วิธีการเข้าร่วม)
- ✅ แสดงรายชื่อผู้ลงทะเบียนทั้งหมด
- ✅ สถิติการเข้าร่วม (ทั้งหมด, พบหน้า, Zoom)
- ✅ **แก้ไขข้อมูลผู้ลงทะเบียน**
- ✅ **ลบข้อมูลผู้ลงทะเบียน**
- ✅ **เก็บข้อมูลใน SQLite Database (ภายใน app)**
- ✅ **Backend API สำหรับจัดการข้อมูล**

## วิธีการใช้งาน

### สำหรับผู้ใช้ทั่วไป

1. เริ่มต้น Backend Server:
   ```bash
   npm install
   npm start
   ```

2. เปิดเบราว์เซอร์ไปที่ `http://localhost:3030`

3. กรอกข้อมูลในฟอร์ม:
   - ชื่อ-นามสกุล
   - วิธีการเข้าร่วม (พบหน้า หรือ Zoom)

4. กดปุ่ม "ลงทะเบียน"

5. ดูรายชื่อผู้ลงทะเบียนและสถิติที่อัปเดตอัตโนมัติ

6. **แก้ไขข้อมูล**: คลิกปุ่ม ✏️ ที่รายการผู้ลงทะเบียน
7. **ลบข้อมูล**: คลิกปุ่ม 🗑️ ที่รายการผู้ลงทะเบียน

### สำหรับ Admin

**เข้าหน้า Admin Dashboard:**
- คลิกปุ่ม "🔐 Admin Dashboard" ที่หน้าแรก หรือ
- เปิดเบราว์เซอร์ไปที่ `http://localhost:3030/admin-login.html`
- **กรอกรหัสผ่าน** (default: `admin123` - ควรเปลี่ยนใน `.env`)

**ฟีเจอร์ในหน้า Admin:**
- 📊 ดูสถิติผู้เข้าร่วมทั้งหมด
- 📋 แสดงรายชื่อผู้ลงทะเบียนในรูปแบบตาราง
- 🔍 ค้นหาข้อมูลผู้ลงทะเบียน
- ✏️ แก้ไขข้อมูลผู้ลงทะเบียน
- 🗑️ ลบข้อมูลผู้ลงทะเบียน
- 📥 ส่งออกข้อมูลเป็นไฟล์ CSV
- 🗑️ ลบข้อมูลทั้งหมด (ระวัง!)
- 🔄 Auto-refresh ทุก 30 วินาที

### สำหรับ Developer

ข้อมูลจะถูกเก็บใน SQLite database (`data/meeting-registration.db`) ซึ่งจะถูกสร้างอัตโนมัติเมื่อเริ่มต้น server

## โครงสร้างโปรเจ็กต์

### Frontend
- `index.html` - โครงสร้างหน้าเว็บ
- `styles.css` - การออกแบบและสไตล์
- `script.js` - ฟังก์ชันการทำงาน (เชื่อมต่อกับ Backend API)

### Backend
- `server.js` - Express server และ API endpoints
- `database.js` - SQLite database management
- `package.json` - Dependencies และ scripts
- `.env` - Environment variables (optional)
- `data/meeting-registration.db` - SQLite database file (สร้างอัตโนมัติ)

## API Endpoints

- `GET /api/participants` - ดึงข้อมูลผู้ลงทะเบียนทั้งหมด
- `POST /api/participants` - เพิ่มผู้ลงทะเบียนใหม่
- `PUT /api/participants/:id` - แก้ไขข้อมูลผู้ลงทะเบียน
- `DELETE /api/participants/:id` - ลบข้อมูลผู้ลงทะเบียน

## การตั้งค่า

### สำหรับ Local Development

1. ติดตั้ง dependencies: `npm install`
2. เริ่มต้น server: `npm start`
3. ฐานข้อมูล SQLite จะถูกสร้างอัตโนมัติในโฟลเดอร์ `data/`

### สำหรับ Production (Synology NAS)

ดูคู่มือการ deploy แบบละเอียดใน [SYNOLOGY_DEPLOY.md](./SYNOLOGY_DEPLOY.md)

**วิธีด่วน:**
1. อัปโหลดไฟล์ทั้งหมดไปยัง Synology NAS
2. ใช้ Docker Compose:
   ```bash
   docker-compose up -d --build
   ```
3. หรือใช้ PM2:
   ```bash
   npm install -g pm2
   npm install --production
   pm2 start ecosystem.config.js
   pm2 save
   ```

## หมายเหตุ

- ข้อมูลจะถูกเก็บใน SQLite database (`data/meeting-registration.db`)
- ฐานข้อมูลจะถูกสร้างอัตโนมัติเมื่อเริ่มต้น server ครั้งแรก
- ข้อมูลจะถูกเก็บถาวรในไฟล์ฐานข้อมูล (ไม่หายเมื่อ restart server)
- Server ต้องรันอยู่เพื่อให้ Frontend ทำงานได้
- **หน้า Admin ต้องใส่รหัสผ่าน** (default: `admin123`)
- เปลี่ยนรหัสผ่านได้ในไฟล์ `.env`: `ADMIN_PASSWORD=your_password`

