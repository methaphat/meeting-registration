# 🚀 คู่มือเริ่มต้นใช้งานอย่างรวดเร็ว

## วิธีที่ 1: ใช้ไฟล์ start.bat (Windows - แนะนำ)

**ง่ายที่สุด!** เพียงดับเบิลคลิกไฟล์ `start.bat` 

ไฟล์นี้จะ:
- ✅ ติดตั้ง dependencies อัตโนมัติ (ถ้ายังไม่มี)
- ✅ เริ่มต้น server อัตโนมัติ
- ✅ แสดง URL ที่ต้องเปิด

---

## วิธีที่ 2: ใช้ Command Line

### 1. เปิด Terminal/Command Prompt

- **Windows**: กด `Win + R` พิมพ์ `cmd` หรือ `powershell` แล้วกด Enter
- **Mac/Linux**: เปิด Terminal

### 2. ไปที่โฟลเดอร์โปรเจ็กต์

```bash
cd "C:\Users\User\OneDrive\Documents\Cursor"
```

### 3. ติดตั้ง Dependencies (ครั้งแรกเท่านั้น)

```bash
npm install
```

คำสั่งนี้จะดาวน์โหลดและติดตั้ง packages ที่จำเป็น:
- express
- cors
- googleapis
- dotenv

### 4. เริ่มต้น Server

```bash
npm start
```

คุณจะเห็นข้อความ:
```
✅ Server is running on http://localhost:3030
📄 Frontend: http://localhost:3030
🔐 Admin: http://localhost:3030/admin.html
```

### 5. เปิดเบราว์เซอร์

เปิดเบราว์เซอร์และไปที่:
- **หน้าแรก**: http://localhost:3030
- **หน้า Admin**: http://localhost:3030/admin.html

---

## ⚠️ หมายเหตุสำคัญ

### ถ้ายังไม่มี Google Sheets Setup

Server จะรันได้ แต่จะ**ไม่สามารถบันทึกข้อมูลลง Google Sheets** ได้

ข้อมูลจะถูกเก็บในหน่วยความจำชั่วคราว (จะหายเมื่อ restart server)

### ถ้าต้องการใช้งาน Google Sheets

1. ดูคู่มือการตั้งค่าในไฟล์ [SETUP.md](./SETUP.md)
2. สร้างไฟล์ `credentials.json` และ `.env`
3. Restart server

---

## 🛠️ การแก้ไขปัญหา

### Error: "Cannot find module 'express'"
**แก้ไข**: รัน `npm install` อีกครั้ง

### Error: "Port 3030 is already in use"
**แก้ไข**: 
- ปิดโปรแกรมอื่นที่ใช้ port 3030 หรือ
- เปลี่ยน port ในไฟล์ `.env`: `PORT=3031`

### Server รันแต่หน้าเว็บไม่แสดง
**แก้ไข**: 
- ตรวจสอบว่าเปิด URL ถูกต้อง: `http://localhost:3030`
- ตรวจสอบว่า server ยังรันอยู่ (ดูที่ terminal)

### ข้อมูลหายเมื่อ restart server
**แก้ไข**: ตั้งค่า Google Sheets API เพื่อเก็บข้อมูลถาวร (ดู [SETUP.md](./SETUP.md))

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# เริ่มต้น server
npm start

# เริ่มต้น server แบบ development (auto-reload)
npm run dev

# หยุด server
กด Ctrl + C ใน terminal
```

---

## ✅ ตรวจสอบว่า Server ทำงาน

1. ดูที่ terminal ควรมีข้อความ: `Server is running on http://localhost:3030`
2. เปิดเบราว์เซอร์ไปที่ `http://localhost:3030`
3. ควรเห็นหน้าเว็บแสดงขึ้นมา

---

## 🎯 ขั้นตอนต่อไป

1. ✅ Server รันแล้ว → ทดสอบลงทะเบียน
2. ⚙️ ตั้งค่า Google Sheets → ดู [SETUP.md](./SETUP.md)
3. 🔐 เข้าหน้า Admin → http://localhost:3030/admin.html

