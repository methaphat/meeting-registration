# 🔧 คู่มือแก้ไขปัญหา

## ปัญหา: รัน start.bat ไม่ได้

### 1. ตรวจสอบ Node.js และ npm

เปิด Command Prompt และรัน:
```bash
node --version
npm --version
```

ถ้าไม่พบ ให้ติดตั้ง Node.js จาก https://nodejs.org/

### 2. ปัญหาเกี่ยวกับ better-sqlite3

**better-sqlite3** ต้อง compile native modules ซึ่งอาจต้องการ build tools บน Windows

#### วิธีแก้ไข:

**วิธีที่ 1: ติดตั้ง Windows Build Tools (แนะนำ)**
```bash
npm install --global windows-build-tools
```

หรือสำหรับ Node.js เวอร์ชันใหม่:
```bash
npm install --global --production windows-build-tools
```

**วิธีที่ 2: ใช้ prebuilt binary**
```bash
npm install better-sqlite3 --build-from-source=false
```

**วิธีที่ 3: ติดตั้ง Visual Studio Build Tools**
1. ดาวน์โหลดจาก: https://visualstudio.microsoft.com/downloads/
2. เลือก "Build Tools for Visual Studio"
3. ติดตั้ง "C++ build tools" workload

**วิธีที่ 4: ใช้ alternative package (ถ้ายังไม่ได้)**
```bash
npm install sqlite3
```

แล้วแก้ไข `database.js` ให้ใช้ `sqlite3` แทน `better-sqlite3`

### 3. ปัญหา Port 3030 ถูกใช้งานอยู่

**ตรวจสอบ:**
```bash
netstat -ano | findstr :3030
```

**แก้ไข:**
- ปิดโปรแกรมอื่นที่ใช้ port 3030 หรือ
- เปลี่ยน port ใน `.env`: `PORT=3031`

### 4. ปัญหาไฟล์หาย

ตรวจสอบว่ามีไฟล์เหล่านี้:
- ✅ `package.json`
- ✅ `server.js`
- ✅ `database.js`
- ✅ `index.html`
- ✅ `script.js`
- ✅ `admin.html`
- ✅ `admin.js`

### 5. ปัญหา Dependencies ติดตั้งไม่สำเร็จ

**ลบและติดตั้งใหม่:**
```bash
rmdir /s /q node_modules
del package-lock.json
npm install
```

### 6. ปัญหา Permission Denied

**รัน Command Prompt as Administrator:**
1. คลิกขวาที่ Command Prompt
2. เลือก "Run as administrator"
3. รัน `start.bat` อีกครั้ง

### 7. ปัญหา Path มีช่องว่างหรืออักขระพิเศษ

ถ้า path มีช่องว่าง (เช่น `OneDrive\Documents\Cursor`) ควรจะไม่มีปัญหา
แต่ถ้ามีปัญหาลอง:
- ย้ายโปรเจ็กต์ไปยัง path ที่ไม่มีช่องว่าง เช่น `C:\projects\meeting-registration`

### 8. วิธีทดสอบทีละขั้นตอน

**ขั้นตอนที่ 1: ติดตั้ง dependencies**
```bash
npm install
```

**ขั้นตอนที่ 2: ทดสอบ database**
```bash
node -e "const db = require('./database'); console.log('Database OK');"
```

**ขั้นตอนที่ 3: เริ่มต้น server**
```bash
npm start
```

### 9. ดู Error Logs

ถ้า start.bat รันไม่ได้ ให้ดู error message ที่แสดงออกมา

**ตัวอย่าง error messages:**

- `'node' is not recognized` → Node.js ไม่ได้ติดตั้ง
- `npm ERR!` → ปัญหาเกี่ยวกับ npm หรือ dependencies
- `Error: Cannot find module` → Dependencies ติดตั้งไม่ครบ
- `EADDRINUSE` → Port ถูกใช้งานอยู่
- `better-sqlite3` build error → ต้องติดตั้ง build tools

### 10. ติดต่อขอความช่วยเหลือ

ถ้ายังแก้ไขไม่ได้ ให้:
1. ดู error message ที่แสดง
2. ตรวจสอบ Node.js version: `node --version`
3. ตรวจสอบ npm version: `npm --version`
4. ตรวจสอบ OS version: `systeminfo | findstr /B /C:"OS Name"`

---

## วิธีทดสอบแบบ Manual

แทนที่จะใช้ `start.bat` สามารถรันทีละขั้นตอน:

```bash
# 1. ติดตั้ง dependencies
npm install

# 2. เริ่มต้น server
npm start
```

หรือใช้ PowerShell:
```powershell
# 1. ติดตั้ง dependencies
npm install

# 2. เริ่มต้น server
npm start
```

